<?php

/*
|--------------------------------------------------------------------------
| Web Routes
|--------------------------------------------------------------------------
|
| The public frontend (routes/frontend.php) owns the `/` route — it renders
| the frontend app. web.php intentionally defines no routes here: a
| duplicate `Route::get('/')` would be dead code, because Laravel resolves
| duplicate GET `/` routes to the last one registered (frontend.php is
| loaded after web.php).
|
*/
